import React from 'react';
import StyledButton from '../components/StyledButton';

const GameContainer = () => {
  return <StyledButton onPress={console.log} text="test" />;
};
export default GameContainer;
